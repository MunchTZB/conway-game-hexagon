class Grid {
  constructor(horizontalCellCount: number, verticalCellCount: number) {
    this.horizontalCellCount = horizontalCellCount;
    this.verticalCellCount = verticalCellCount;
  }

  init() {

  }
}

export default Grid;
